# Backend
PORT=4000
DATABASE_URL=postgres://pipeflow:pipeflow@localhost:5432/pipeflow
AGENT_TOKEN=dev-token-change-me
# CORS: '*' = allow any origin, comma-list = allow-list, single value = exact
CORS_ORIGIN=*

# Frontend (apps/web/.env)
VITE_API_URL=http://localhost:4000
VITE_SOCKET_URL=http://localhost:4000
